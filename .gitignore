
!.env.example
# `/~/` anchors to a TOP-LEVEL entry literally named `~` only. Per
# actually wants to track one). `AGENTS.md` is NOT in this block on
# Agent CLI sandbox (Codex hooks + hooks.json under .codex/ — never
# Air live-reload build output
# Backend build artefacts
# Build SHA stamp (generated by CI deploy.yml, compiled into binary)
# Claude / agent
# Devtools / one-shot scripts (not part of the app)
# Editor / IDE
# Foundry
# Foundry (audit install only — never commit lib/)
# from inheriting on a fresh clone, so it IS tracked.
# Go dependencies (module cache lives in GOPATH)
# gstack browser logs
# integration test run, never tracked.
# Literal tilde directory at repo root (dev-sandbox leak). This does
# Local-only dev/AI tooling (kept on disk, never in repo)
# NOT match the OS home directory — gitignore has no shell expansion;
# NUL device files (Windows artifact)
# OS
# project policy this is dev-only on the working machine.
# purpose: it's the per-project AI rules file every teammate benefits
# Root-level rendered HTML build artifacts (do NOT touch frontend/templates/)
# Secrets / env
# seed-testnet built binary (run via `go run` from tools/seed-testnet)
# Single binary output
# Stale SVG duplicates — now at frontend/static/nft/
# svggen — standalone SVG art generator (moved to ~/Desktop/svggen/)
# temp_gen — temporary generation output directory
# Test fixture for the NFT picker upload pipeline — replaced on every
# tracked; force-add per-repo with `git add -f .codex/` if a project
# Was part of repo for testnet seeding; now a separate standalone tool.
**/.env.local
**/secrets.*
*.key
*.p12
*.pem
*.pfx
*.swo
*.swp
.claude/
.claude/settings.local.json
.codex/
.DS_Store
.env
.env.*
.gstack/
.idea/
.vscode/
/*.html
/~/
/go.mod
/go.sum
/main.go
app/.env
app/nul
backend/*.exe
backend/.env
backend/api_server
backend/bin/
backend/internal/api/build_sha_gen.go
backend/internal/hot/lib/
backend/internal/ui/static/uploads/
backend/server
backend/server.exe
backend/tmp/
backend/vendor/
bin/
CLAUDE.md
contracts/.env
contracts/broadcast/
contracts/cache/
contracts/foundry.lock
contracts/lib/
contracts/out/
desktop.ini
dev.ps1
devtools/
docs/PERFORMANCE_AUDIT.md
docs/REVIEW_PRODUCTION_HARDENING.md
frontend/static/svggen/
magicwebb.exe
magicwebb_win.exe
nul
svggen/
temp_gen/
Thumbs.db
TODOS.md
tools/seed-testnet/seed-testnet
